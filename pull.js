// Pull from IPFS to your local filesystem

// Initialize IPFS

// Take IPFS/IPNS URL
// Convert to content path
// If IPNS, resolve it
// Take FS path

// Do a recursive LS / compare with the local FS
// Check size and mtime, if same skip over entry (probably same)

// Output changes as they are detected
